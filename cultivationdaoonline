local function a()
  for b, c in ipairs(game:GetService("CoreGui"):GetDescendants()) do
    if (c:IsA("ScreenGui") and (c.Name == "Obsidian")) then
      c:Destroy()
      for d, e in ipairs(game:GetService("Players").LocalPlayer.PlayerGui:GetDescendants()) do
        if (e:isA("ScreenGui") and (e.Name == "GearTeleportGui")) then
          e:Destroy()
        end
      end
    end
  end
end
a()
local f = "loadstring(game:HttpGet('https://raw.githubusercontent.com/yue-os/2026robloxscripts/refs/heads/main/CultivationOnline.luau'))()"
local g = loadstring(game:HttpGet("https://raw.githubusercontent.com/yue-os/2026robloxscripts/refs/heads/main/GameDatas.lua"))()
local h = loadstring(game:HttpGet("https://raw.githubusercontent.com/yue-os/ObsidianUi/refs/heads/test/Library.lua"))()
local i = loadstring(game:HttpGet("https://raw.githubusercontent.com/yue-os/ObsidianUi/refs/heads/main/addons/SaveManager.lua"))()
local j = loadstring(game:HttpGet("https://raw.githubusercontent.com/yue-os/ObsidianUi/refs/heads/main/addons/ThemeManager.lua"))()
local Players = game:GetService("Players")
local k = game:GetService("ReplicatedStorage")
local l = Players.LocalPlayer
local workspace = game:GetService("Workspace")
local m = l:WaitForChild("PlayerGui")
local n = m:WaitForChild("Main")
local o = n:WaitForChild("Frames"):WaitForChild("Guide"):WaitForChild("Container")
local p = game:GetService("VirtualUser")
local q = game:GetService("HttpService")
local r = game:GetService("TeleportService")
local s = game.JobId
local t = game.PlaceId
local u = workspace:WaitForChild("Mobs")
local v = workspace:WaitForChild("Herbs")
local w = game:GetService("Players").LocalPlayer.PlayerGui.Main.Frames.Shop.BodyConstitution.Current
local y, z = pcall(function()
  local x = game:GetService("CoreGui").RobloxGui:FindFirstChild("CoreScripts/NetworkPause")
  if x then
    x:Destroy()
  end
end)
local function aa()
  if (type(identifyexecutor) == "function") then
    local ab, ac = pcall(identifyexecutor)
    if (ab and ac) then
      return ac
    end
  end
  if (type(getexecutorname) == "function") then
    local ad, ae = pcall(getexecutorname)
    if (ad and ae) then
      return ae
    end
  end
  return "Unknown"
end
local af = aa()
local ag = (string.find(string.lower(af), "delta") ~= nil)
local ah
if ag then
  print("🚀 Delta detected! Loading native PlayerService Replica...")
  pcall(function()
    ah = require(k.Shared.Services.PlayerService.PlayerServiceClient)
  end)
end
local ai = {}
local aj = false
local ak = false
local al = g.zoneCFrames[1]
local am = {}
local an = {}
for ao = 1, 6 do
  local ap = ("BodyZones" .. tostring(ao))
  local aq = o:FindFirstChild(ap)
  if aq then
    local ar = aq:FindFirstChild("Title")
    local as = aq:FindFirstChild("Subtitle")
    if (ar and as) then
      local at = (ar.Text .. (" | " .. as.Text))
      table.insert(an, at)
      am[at] = g.zoneCFrames[ao]
    end
  end
end
local function au(av)
  local aw = l.Character
  if (aw and aw:FindFirstChild("HumanoidRootPart")) then
    local ax = aw.HumanoidRootPart
    ax.CFrame = (av.CFrame + Vector3.new(0, 5, 0))
    ax.AssemblyLinearVelocity = Vector3.zero
    ax.AssemblyAngularVelocity = Vector3.zero
  end
end
local function ay(az)
  if ((az and az:FindFirstChild("HumanoidRootPart")) and al) then
    az.HumanoidRootPart.CFrame = al
  end
end
local ba = {}
local function bb(bc)
  if ((((bc:IsA("Model") or bc:IsA("Folder"))) and bc.Parent) and (bc.Parent.Parent == u)) then
    local bd = bc.Parent.Name
    if not ba[bd] then
      ba[bd] = {}
    end
    if not table.find(ba[bd], bc) then
      table.insert(ba[bd], bc)
    end
  end
end
local function be(bf)
  if ((((bf:IsA("Model") or bf:IsA("Folder"))) and bf.Parent) and (bf.Parent.Parent == u)) then
    local bg = bf.Parent.Name
    if ba[bg] then
      local bh = table.find(ba[bg], bf)
      if bh then
        table.remove(ba[bg], bh)
      end
    end
  end
end
for bi, bj in ipairs(u:GetChildren()) do
  if (bj:IsA("Folder") or bj:IsA("Model")) then
    for bk, bl in ipairs(bj:GetChildren()) do
      bb(bl)
    end
  end
end
u.DescendantAdded:Connect(bb)
u.DescendantRemoving:Connect(be)
local function bm(bn, bo)
  bo = (bo or 4)
  local bp = l.Character
  if not bp then
    return
  end
  local bq = bp:FindFirstChild("HumanoidRootPart")
  local br = bn:FindFirstChild("HumanoidRootPart")
  if (bq and br) then
    local bs = ((br.CFrame * CFrame.new(0, 0, bo))).Position
    local bt = RaycastParams.new()
    bt.FilterDescendantsInstances = {bp, bn}
    bt.FilterType = Enum.RaycastFilterType.Exclude
    local bu = workspace:Raycast(bs, Vector3.new(0, -500, 0), bt)
    local bv = bs.Y
    if bu then
      bv = (bu.Position.Y + 3)
    end
    local bw = Vector3.new(bs.X, bv, bs.Z)
    local bx = Vector3.new(br.Position.X, bw.Y, br.Position.Z)
    bq.CFrame = CFrame.new(bw, bx)
    bq.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
    bq.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
  end
end
local function by(bz)
  local ca = ("\t" .. (string.pack("<I2", #bz) .. bz))
  return {buffer.fromstring(ca)}
end
local function cb(cc, cd, ce)
  local cf = ("\1" .. (string.pack("<I2", #cc) .. (cc .. (string.pack("<I4", cd) .. (string.pack("<I2", #("Buy")) .. ("Buy" .. (string.pack("<I2", #ce) .. ce)))))))
  return {buffer.fromstring(cf)}
end
local function cg(ch)
  local ci = ("\1" .. (string.pack("<I2", #ch) .. (ch .. (string.pack("<I4", 2) .. (string.pack("<I2", #("Sell")) .. ("Sell" .. (string.pack("<I2", #("Grassland")) .. "Grassland")))))))
  return {buffer.fromstring(ci)}
end
local function cj(ck)
  local cl = ("\27" .. (string.pack("<I2", #ck) .. ck))
  return {buffer.fromstring(cl)}
end
local function cm()
  local cn = {}
  local co = game:HttpGet(("https://games.roblox.com/v1/games/" .. (t .. "/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true")))
  local cp = q:JSONDecode(co)
  if (cp and cp.data) then
    for cq, cr in next, cp.data do
      if (((((type(cr) == "table") and tonumber(cr.playing)) and tonumber(cr.maxPlayers)) and (cr.playing < cr.maxPlayers)) and (cr.id ~= s)) then
        table.insert(cn, 1, cr.id)
      end
    end
  end
  if (#cn > 0) then
    r:TeleportToPlaceInstance(t, cn[math.random(1, #cn)], l)
  else
    h:Notify("Serverhop: Couldn't find a server.")
  end
end
local function cs()
  local ct = {}
  for cu, cv in pairs(g.ItemConfig) do
    table.insert(ct, cu)
  end
  return ct
end
local function cw(cx)
  if (type(cx) ~= "string") then
    return 0
  end
  cx = string.lower(string.gsub(cx, "[,%s]", ""))
  local cy, cz = string.match(cx, "^([%d%.]+)([a-z]*)$")
  if not cy then
    return 0
  end
  local da = (tonumber(cy) or 0)
  local db = {k = 1e3, m = 1e6, b = 1e9, t = 1e12, qa = 1e15, qi = 1e18, sx = 1e21, sp = 1e24, oc = 1e27, no = 1e30, dc = 1e33}
  if ((cz and (cz ~= "")) and db[cz]) then
    return (da * db[cz])
  end
  return da
end
local function dc(dd)
  if (type(dd) ~= "string") then
    return ""
  end
  local de = string.split(dd, "[")[1]
  if de then
    return string.match(de, "^%s*(.-)%s*$")
  end
  return dd
end
local function df()
  local dg = {Qi = 0, Realm = 1, Health = 0, Body = 1}
  if (ag and ah) then
    local dh, di = pcall(ah.GetData)
    if (dh and (type(di) == "table")) then
      dg.Qi = ((tonumber(di.Qi) or tonumber(di.qi)) or dg.Qi)
      dg.Health = (((tonumber(di.Health) or tonumber(di.HP)) or tonumber(di.Hp)) or dg.Health)
      dg.Realm = ((tonumber(di.Realm) or tonumber(di.RealmLevel)) or dg.Realm)
      dg.Body = ((tonumber(di.Body) or tonumber(di.BodyLevel)) or dg.Body)
      dg.SpiritRoot = ((di.SpiritRoot or di.Root) or di.spiritRoot)
      dg.BodyConstitution = ((di.BodyConstitution or di.Constitution) or di.bodyConstitution)
      if (di.Qi or di.qi) then
        return dg
      end
    end
  end
  if (not m or not n) then
    return dg
  end
  local dj = (((n:FindFirstChild("HUD") and n.HUD:FindFirstChild("Vitals")) and n.HUD.Vitals:FindFirstChild("Qi")) and n.HUD.Vitals.Qi:FindFirstChild("Amount"))
  if (dj and dj:IsA("TextLabel")) then
    dg.Qi = cw(dj.Text)
  end
  local dk = (((n:FindFirstChild("HUD") and n.HUD:FindFirstChild("Vitals")) and n.HUD.Vitals:FindFirstChild("HP")) and n.HUD.Vitals.HP:FindFirstChild("Amount"))
  if (dk and dk:IsA("TextLabel")) then
    dg.Health = cw(dk.Text)
  end
  for dl, dm in pairs(workspace.Characters:GetChildren()) do
    if (dm.Name == l.Name) then
      local dn = dm:GetAttribute("Body")
      if dn then
        dg.Body = tonumber(dn)
      end
      local dp = dm:GetAttribute("Realm")
      if dp then
        dg.Realm = tonumber(dp)
      end
      break
    end
  end
  local dq = (((n:FindFirstChild("Frames") and n.Frames:FindFirstChild("Shop")) and n.Frames.Shop:FindFirstChild("SpiritRoot")) and n.Frames.Shop.SpiritRoot:FindFirstChild("Current"))
  if (dq and dq:IsA("TextLabel")) then
    dg.SpiritRoot = dc(dq.Text)
  end
  local dr = (((n:FindFirstChild("Frames") and n.Frames:FindFirstChild("Shop")) and n.Frames.Shop:FindFirstChild("BodyConstitution")) and n.Frames.Shop.BodyConstitution:FindFirstChild("Current"))
  if (dr and dr:IsA("TextLabel")) then
    dg.BodyConstitution = dc(dr.Text)
  end
  return dg
end
local function ds()
  local dt = {}
  for du, dv in ipairs(g.Roots) do
    table.insert(dt, dv[1])
  end
  return dt
end
local function dw()
  local dx = {}
  for dy, dz in ipairs(g.Constitution) do
    table.insert(dx, dz[1])
  end
  return dx
end
local function ea()
  local eb = {}
  local ec = {}
  for ed = 1, 9 do
    local ee = ("QiZones" .. tostring(ed))
    local ef = o:FindFirstChild(ee)
    if ef then
      local eg = ef:FindFirstChild("Title")
      local eh = ef:FindFirstChild("Subtitle")
      if (eg and eh) then
        local ei = (eg.Text .. (" | " .. eh.Text))
        table.insert(eb, ei)
        ec[ei] = g.qiZoneCFrames[ed]
      end
    end
  end
  return eb, ec
end
local function ej(ek)
  local el = math.ceil((ek / 10))
  if (el > 31) then
    el = 31
  end
  if (el < 1) then
    el = 1
  end
  local em = g.realmBaseQi[el]
  local en = (em * ((1.15 ^ ((ek - 1)))))
  return math.floor(en)
end
local function eo(ep)
  local eq = math.ceil((ep / 10))
  if (eq > 33) then
    eq = 33
  end
  if (eq < 1) then
    eq = 1
  end
  local er = g.realmBaseBody[eq]
  local es = (er * ((1.11 ^ ((ep - 1)))))
  return math.floor(es)
end
l.Idled:Connect(function()
  p:CaptureController()
  p:ClickButton2(Vector2.new())
end)
local function et()
  if (syn and syn.queue_on_teleport) then
    syn.queue_on_teleport(f)
  elseif queue_on_teleport then
    queue_on_teleport(f)
  else
    warn("Your executor does not support queue_on_teleport")
  end
end
local function eu()
  local ev = {}
  if not m then
    return ev
  end
  local ew = (((m:FindFirstChild("Main") and m.Main:FindFirstChild("Frames")) and m.Main.Frames:FindFirstChild("Inventory")) and m.Main.Frames.Inventory:FindFirstChild("Container"))
  if ew then
    for ex, ey in ipairs(ew:GetChildren()) do
      if (ey:IsA("TextButton") and (ey.Name ~= "Template")) then
        table.insert(ev, ey.Name)
      end
    end
  end
  return ev
end
local function ez(fa)
  local fb = ("\19" .. (string.pack("<I2", #fa) .. fa))
  return {buffer.fromstring(fb), nil}
end
local function fc(fd, fe)
  local ff = tostring(fd)
  local fg = ("\20" .. (string.pack("<I2", #ff) .. (ff .. (string.pack("<I2", #fe) .. fe))))
  return {buffer.fromstring(fg)}
end
local fh = {600, 1200, 2400, 3600, 4800, 10800}
local function fi(fj)
  local fk = (tostring(fj) .. ":EventPlaytime")
  local fl = ("\20" .. (string.pack("<I2", #fk) .. fk))
  return {buffer.fromstring(fl)}
end
local function fm()
  local fn = {}
  local fo = {}
  for fp, fq in ipairs(u:GetChildren()) do
    if (fq:IsA("Folder") or fq:IsA("Model")) then
      for fr, fs in ipairs(fq:GetChildren()) do
        if ((fs:IsA("Model") and fs.Name) and not fo[fs.Name]) then
          fo[fs.Name] = true
          table.insert(fn, fs.Name)
        end
      end
    end
  end
  table.sort(fn)
  return fn
end
local ft = fm()
local fu = {}
local function fv(fw)
  for fx, fy in pairs(ba) do
    for fz, ga in ipairs(fy) do
      if (ga.Name == fw) then
        local gb = ga:FindFirstChild("Humanoid")
        local gc = ga:FindFirstChild("HumanoidRootPart")
        if ((gb and (gb.Health > 0)) and gc) then
          return ga
        end
      end
    end
  end
  return nil
end
local function gd()
  local ge = {}
  for gf, gg in ipairs(fu) do
    ge[gg] = true
  end
  for gh, gi in pairs(ba) do
    for gj, gk in ipairs(gi) do
      if ge[gk.Name] then
        local gl = gk:FindFirstChild("Humanoid")
        local gm = gk:FindFirstChild("HumanoidRootPart")
        if ((gl and (gl.Health > 0)) and gm) then
          return gk
        end
      end
    end
  end
  return nil
end
local function gn(go)
  task.spawn(function()
    while go do
      for gp, gq in ipairs(workspace:GetDescendants()) do
        if gq:IsA("ProximityPrompt") then
          if (gq.HoldDuration ~= 0) then
            gq.HoldDuration = 0
          end
        end
      end
      task.wait(1)
    end
  end)
end
Players.LocalPlayer.OnTeleport:Connect(function(gr)
  et()
end)
Players.PlayerAdded:Connect(function(gs)
  gn(true)
end)
local gt = h:CreateWindow({Title = "Y-Hub", Center = true, Icon = 128171209017859, AutoShow = true, Size = UDim2.fromOffset(650, 460), ShowCustomCursor = false, ToggleKeybind = Enum.KeyCode.LeftControl})
local gu = gt:AddTab("Main", "book-user")
local gv = gt:AddTab("Misc", "server")
local gw = gt:AddTab("Settings", "cog")
local gx = gu:AddLeftGroupbox("Spin")
local gy = gu:AddRightGroupbox("Alchemy")
local gz = gu:AddRightGroupbox("Qi and Training")
local ha = gu:AddLeftGroupbox("Merchant")
local hb = gu:AddRightGroupbox("Quests")
local hc = gv:AddLeftGroupbox("Experimental")
local hd = gv:AddRightGroupbox("Playtime Rewards")
local hf = gx:AddDropdown("Roots", {Values = ds(), Default = {}, Multi = true, Text = "Select Root", Tooltip = "Select the root you want to spin to.", Callback = function(he)

end})
local ht = gx:AddToggle("Spin Root", {Text = "Spin Root", Tooltip = "Automatically spins your root.", Callback = function(hg)
  ak = hg
  task.spawn(function()
    while ak do
      local hh = hf.Value
      local hi = df()
      if ((hi and hi.SpiritRoot) and hh[hi.SpiritRoot]) then
        h:Notify(("🎉 Successfully rolled Root: " .. tostring(hi.SpiritRoot)))
        ak = false
        break
      end
      if n then
        local hj = (((n:FindFirstChild("Frames") and n.Frames:FindFirstChild("Shop")) and n.Frames.Shop:FindFirstChild("SpiritRoot")) and n.Frames.Shop.SpiritRoot:FindFirstChild("Skip"))
        if (hj and hj:IsA("GuiButton")) then
          local hk = hj.BackgroundColor3
          local hl = math.round((hk.R * 255))
          local hm = math.round((hk.G * 255))
          local hn = math.round((hk.B * 255))
          if (((hl == 255) and (hm == 42)) and (hn == 42)) then
            if getconnections then
              for ho, hp in ipairs(getconnections(hj.MouseButton1Click)) do
                hp:Fire()
              end
              for hq, hr in ipairs(getconnections(hj.Activated)) do
                hr:Fire()
              end
            end
          end
        end
      end
      local hs = {buffer.fromstring("\30\0"), hh}
      k.ByteNetReliable:FireServer(unpack(hs))
      task.wait(0.5)
    end
  end)
end})
local hv = gx:AddDropdown("Constitution", {Values = dw(), Default = {}, Multi = true, Text = "Select Constitution", Tooltip = "Select the constitution you want to spin to.", Callback = function(hu)

end})
local ik = gx:AddToggle("Spin Constitution", {Text = "Spin Constitution", Tooltip = "Automatically spins your constitution.", Callback = function(hw)
  aj = hw
  task.spawn(function()
    while aj do
      local hx = hv.Value
      local hy = df()
      if ((hy and hy.BodyConstitution) and hx[hy.BodyConstitution]) then
        h:Notify(("🎉 Successfully rolled Constitution: " .. tostring(hy.BodyConstitution)))
        aj = false
        break
      end
      if n then
        local hz = (((n:FindFirstChild("Frames") and n.Frames:FindFirstChild("Shop")) and n.Frames.Shop:FindFirstChild("BodyConstitution")) and n.Frames.Shop.BodyConstitution:FindFirstChild("Skip"))
        if (hz and hz:IsA("GuiButton")) then
          local ia = hz.BackgroundColor3
          local ib = math.round((ia.R * 255))
          local ic = math.round((ia.G * 255))
          local id = math.round((ia.B * 255))
          if (((ib == 255) and (ic == 42)) and (id == 42)) then
            if getconnections then
              for ie, ig in ipairs(getconnections(hz.MouseButton1Click)) do
                ig:Fire()
              end
              for ih, ii in ipairs(getconnections(hz.Activated)) do
                ii:Fire()
              end
            end
          end
        end
      end
      local ij = {buffer.fromstring("\31\0"), hx}
      k.ByteNetReliable:FireServer(unpack(ij))
      task.wait(0.5)
    end
  end)
end})
local il = "Ginseng"
local im = g.ItemConfig[il].Location[1]
local io = {}
local ip = 1
local it = ha:AddDropdown("Merchant Item", {Values = cs(), Default = {}, Multi = true, Text = "Select Items", Tooltip = "Select multiple items you want to auto buy.", Searchable = true, Callback = function(iq)
  io = {}
  for ir, is in pairs(iq) do
    if is then
      table.insert(io, ir)
    end
  end
end})
local iv = ha:AddSlider("Amount", {Text = "Amount", Min = 1, Max = 100, Default = 1, Rounding = 0, Callback = function(iu)
  ip = iu
end})
local iw = false
local ix = false
local iy = {}
local jm = ha:AddToggle("Auto Buy", {Text = "Auto Buy", Tooltip = "Teleports once to each necessary merchant per game, then buys selected items.", Callback = function(iz)
  iw = iz
  task.spawn(function()
    while iw do
      for ja, jb in ipairs(io) do
        if not iw then
          break
        end
        local jc = g.ItemConfig[jb]
        if (jc and jc.Location) then
          for jd, je in ipairs(jc.Location) do
            if not iw then
              break
            end
            if (g.MerchantLocations[je] and not iy[je]) then
              local jf = l.Character
              local jg = (jf and jf:FindFirstChild("HumanoidRootPart"))
              if jg then
                print(("🚀 Teleporting to " .. (je .. " Merchant (First time only)...")))
                jg.CFrame = g.MerchantLocations[je]
                iy[je] = true
                task.wait(1.5)
              end
            end
            local jh = l.PlayerGui.Main.Frames.Merchant.Container:FindFirstChild(jb)
            local ji = 0
            if (jh and jh:FindFirstChild("Stock")) then
              local jj = string.match(jh.Stock.Text, "%d+")
              if jj then
                ji = tonumber(jj)
              end
            end
            if (ji > 0) then
              local jk = math.min(ip, ji)
              local jl = cb(jb, jk, je)
              k.ByteNetReliable:FireServer(unpack(jl))
              task.wait(0.25)
            end
          end
        end
      end
      task.wait(1)
    end
  end)
end})
local jq = hb:AddDropdown("SpecificMob", {Values = ft, Default = {}, Multi = true, Text = "Select Specific Mobs", Tooltip = "Select multiple mobs in the game to farm.", Searchable = true, Callback = function(jn)
  fu = {}
  for jo, jp in pairs(jn) do
    if jp then
      table.insert(fu, jo)
    end
  end
end})
local jr = false
hb:AddToggle("AutoFarmSpecificMobToggle", {Text = "Auto Farm Specific Mobs", Tooltip = "Continuously hunts down any of the selected mobs until they are dead.", Callback = function(js)
  jr = js
  task.spawn(function()
    while jr do
      local jt = gd()
      if jt then
        local ju = jt:FindFirstChild("Humanoid")
        while (((jr and jt.Parent) and ju) and (ju.Health > 0)) do
          bm(jt, 4)
          task.wait(0.1)
        end
      else
        task.wait(1)
      end
      task.wait(0.1)
    end
  end)
end})
hb:AddButton({Text = "Refresh Mob List", DoubleClick = false, Tooltip = "Refreshes the dropdown with any newly spawned mobs in your chunk.", Func = function()
  local jv = fm()
  jq:SetValues(jv)
  h:Notify("🔄 Mob list refreshed!")
end})
local jw = g.QuestConfig[1]
local ka = hb:AddDropdown("Quest", {Values = g.QuestConfig, Default = 1, Multi = false, Text = "Select Quest", Tooltip = "Select the quest you want to complete.", Searchable = true, Callback = function(jx)
  jw = jx
  local jy = {buffer.fromstring("\t\6\0Cancel")}
  k.ByteNetReliable:FireServer(unpack(jy))
  task.wait(0.25)
  local jz = by(jx)
  k.ByteNetReliable:FireServer(unpack(jz))
end})
ka:SetValue(jw)
local kb = false
local function kc(kd)
  local ke = g.QuestMap[kd]
  if not ke then
    local kf = ba[kd]
    if (kf and (#kf > 0)) then
      for kg, kh in ipairs(kf) do
        local ki = kh:FindFirstChild("Humanoid")
        local kj = kh:FindFirstChild("HumanoidRootPart")
        if ((ki and (ki.Health > 0)) and kj) then
          return kh
        end
      end
    end
    return nil
  end
  local kk = ba[ke.Folder]
  if (kk and (#kk > 0)) then
    for kl, km in ipairs(kk) do
      if (km.Name == ke.MobName) then
        local kn = km:FindFirstChild("Humanoid")
        local ko = km:FindFirstChild("HumanoidRootPart")
        if ((kn and (kn.Health > 0)) and ko) then
          return km
        end
      end
    end
  end
  return nil
end
hb:AddToggle("AutoFarmToggle", {Text = "Enable Auto Farm", Tooltip = "Automatically teleports to and farms the selected quest.", Callback = function(kp)
  kb = kp
  task.spawn(function()
    while kb do
      local kq = kc(jw)
      if kq then
        bm(kq, 4)
      else
        task.wait(1)
      end
      task.wait(0.1)
    end
  end)
end})
local kr = false
hb:AddToggle("AutoKillBossesToggle", {Text = "Auto Kill All Bosses", Tooltip = "Teleports to spawn points to load the chunk, then hunts the boss.", Callback = function(ks)
  kr = ks
  task.spawn(function()
    while kr do
      for kt, ku in ipairs(g.bossData) do
        if not kr then
          break
        end
        local kv = l.Character
        if (kv and kv:FindFirstChild("HumanoidRootPart")) then
          kv.HumanoidRootPart.CFrame = ku.CF
          task.wait(1.5)
          if not kr then
            break
          end
          local kw = nil
          for kx, ky in ipairs(u:GetChildren()) do
            for kz, la in ipairs(ky:GetChildren()) do
              if ((la.Name == ku.Name) and la:FindFirstChild("HumanoidRootPart")) then
                local lb = la:FindFirstChild("Humanoid")
                if (lb and (lb.Health > 0)) then
                  kw = la
                  break
                end
              end
            end
            if kw then
              break
            end
          end
          if kw then
            local lc = kw:FindFirstChild("Humanoid")
            print(("⚔️ " .. (ku.Name .. " loaded! Engaging...")))
            local ld = false
            while (((kw.Parent and lc) and (lc.Health > 0)) and kr) do
              local le = l.Character
              local lf = (le and le:FindFirstChild("Humanoid"))
              if ((not le or not lf) or (lf.Health <= 0)) then
                ld = true
                break
              end
              if not kw:FindFirstChild("HumanoidRootPart") then
                break
              end
              bm(kw, 4)
              task.wait(0.1)
            end
            if ld then
              print(("☠️ You died to " .. (ku.Name .. ". Waiting for respawn...")))
              l.CharacterAdded:Wait()
              task.wait(2)
            else
              print(("💀 " .. (ku.Name .. " defeated!")))
              task.wait(0.5)
            end
          else
            print(("👻 " .. (ku.Name .. " has not respawned yet.")))
          end
        end
      end
      task.wait(2)
    end
  end)
end})
hc:AddToggle("AutoKillBossesAndHopToggle", {Text = "Auto Farm Bosses & Hop", Tooltip = "Hunts target bosses, then server hops if all are dead.", Callback = function(lg)
  kr = lg
  task.spawn(function()
    while kr do
      local lh = false
      local li = {["Golem Boss"] = true, ["Maniac Sword Cultivator"] = true, ["Ember Golem"] = true}
      for lj, lk in ipairs(g.bossData) do
        if not kr then
          break
        end
        local ll = l.Character
        if (ll and ll:FindFirstChild("HumanoidRootPart")) then
          ll.HumanoidRootPart.CFrame = lk.CF
          task.wait(1.5)
          if not kr then
            break
          end
          local lm = nil
          for ln, lo in ipairs(u:GetChildren()) do
            for lp, lq in ipairs(lo:GetChildren()) do
              if ((lq.Name == lk.Name) and lq:FindFirstChild("HumanoidRootPart")) then
                local lr = lq:FindFirstChild("Humanoid")
                if (lr and (lr.Health > 0)) then
                  lm = lq
                  break
                end
              end
            end
            if lm then
              break
            end
          end
          if lm then
            lh = true
            local ls = lm:FindFirstChild("Humanoid")
            print(("⚔️ " .. (lk.Name .. " found!")))
            while (((lm.Parent and ls) and (ls.Health > 0)) and kr) do
              bm(lm, 4)
              task.wait(0.1)
            end
          else
            print(("👻 " .. (lk.Name .. " is dead or missing.")))
          end
        end
      end
      if (kr and not lh) then
        h:Notify("🔄 All target bosses dead/missing. Server hopping...")
        cm()
      end
      task.wait(2)
    end
  end)
end})
local lt = eu()
local lu = {}
local ly = hc:AddDropdown("MyFilteredDropdown", {Values = lt, Default = {}, Multi = true, Text = "Select Items to Sell", Tooltip = "Select multiple owned items to sell (Warn: This will sell all the selected items at max quantity).", Searchable = true, Callback = function(lv)
  lu = {}
  for lw, lx in pairs(lv) do
    if lx then
      table.insert(lu, lw)
    end
  end
end})
local lz = false
hc:AddToggle("AutoSellSelectedToggle", {Text = "Auto Sell Selected", Tooltip = "Loops through the selected items and sell.", Default = false, Callback = function(ma)
  lz = ma
  if lz then
    task.spawn(function()
      while lz do
        for mb, mc in ipairs(lu) do
          if not lz then
            break
          end
          local md = cg(mc)
          k.ByteNetReliable:FireServer(unpack(md))
          task.wait(0.25)
        end
        task.wait(1)
      end
    end)
  end
end})
local mf = gy:AddDropdown("HerbTargetSelect", {Values = g.herbOptions, Default = 0, Multi = true, Text = "Select Herbs to Harvest", Tooltip = "Select one or more herbs to target.", Callback = function(me)
  ai = me
end})
gy:AddSlider("Harvest Delay", {Text = "Harvest Delay (seconds)", Min = 0.1, Max = 5, Default = 0.5, Rounding = 1, Tooltip = "Set the delay between harvesting herbs.", Callback = function(mg)
  harvestDelay = mg
end})
local mh = false
local mi = {}
gy:AddToggle("AutoHarvestSpecificToggle", {Text = "Auto Harvest", Tooltip = "Teleports to and harvests ONLY the herbs you selected above.", Callback = function(mj)
  mh = mj
  task.spawn(function()
    while mh do
      task.wait(5)
      mi = {}
    end
  end)
  task.spawn(function()
    while mh do
      local mk = false
      for ml, mm in ipairs(v:GetChildren()) do
        if not mh then
          break
        end
        if ((mm:IsA("MeshPart") and not mi[mm]) and ai[mm.Name]) then
          local mn = mm:FindFirstChildWhichIsA("ProximityPrompt")
          if (mn and mn.Enabled) then
            mk = true
            au(mm)
            task.wait(0.2)
            fireproximityprompt(mn)
            mi[mm] = true
            task.wait(harvestDelay)
          end
        end
      end
      if not mk then
        task.wait(0.5)
      else
        task.wait()
      end
    end
  end)
end})
local mq = gz:AddDropdown("Body Zone", {Values = an, Default = 1, Multi = false, Text = "Select Body Zone", Tooltip = "Select the body zone you want to train in.", Callback = function(mo)
  local mp = table.find(an, mo)
  if (mp and g.zoneCFrames[mp]) then
    al = am[mo]
  end
end})
local mr = 12
local mt = gz:AddSlider("HP Threshold", {Text = "HP Threshold %", Min = 1, Max = 100, Default = 12, Rounding = 0, Tooltip = "Set the HP percentage threshold to trigger a teleport and retrain.", Callback = function(ms)
  mr = ms
end})
local mu = false
gz:AddToggle("AutoTrainToggle", {Text = "Enable Auto Train", Tooltip = "Teleports to zone, triggers training once, then maintains boost until HP is low.", Callback = function(mv)
  mu = mv
  task.spawn(function()
    if mu then
      ay(l.Character)
      task.wait(0.2)
      local mw = {buffer.fromstring("\22"), nil}
      k.ByteNetReliable:FireServer(unpack(mw))
    end
    while mu do
      local mx = (l.Character or l.CharacterAdded:Wait())
      local my = mx:FindFirstChild("Humanoid")
      if (my and (my.Health > 0)) then
        local mz = (((my.Health / my.MaxHealth)) * 100)
        if (mz < mr) then
          my.Health = 0
          l.CharacterAdded:Wait()
          task.wait(2)
          mx = l.Character
          my = mx:FindFirstChild("Humanoid")
          ay(l.Character)
          task.wait(0.2)
          local na = {buffer.fromstring("\22"), nil}
          k.ByteNetReliable:FireServer(unpack(na))
        else
          local nb = {buffer.fromstring("\n\b\0Training")}
          k.ByteNetReliable:FireServer(unpack(nb))
          task.wait(0.5)
        end
      else
        task.wait(0.5)
      end
    end
  end)
end})
local nc = g.alchemyOptions[1]
local nd = false
local nf = gy:AddDropdown("Alchemy Pill", {Values = g.alchemyOptions, Default = nc, Multi = false, Text = "Select Pill", Tooltip = "Select the pill you want to craft.", Searchable = true, Callback = function(ne)
  nc = ne
end})
gy:AddToggle("AutoAlchemyToggle", {Text = "Enable Auto Alchemy", Tooltip = "Automatically crafts the selected pill if you have the materials.", Callback = function(ng)
  nd = ng
  task.spawn(function()
    while nd do
      if nc then
        local nh = cj(nc)
        print(("🧪 Attempting to craft: " .. nc))
        k.ByteNetReliable:FireServer(unpack(nh))
      end
      task.wait(1)
    end
  end)
end})
local ni, nj = ea()
local nl = gz:AddDropdown("Qi Zone", {Values = ni, Default = 1, Multi = false, Text = "Select Qi Zone", Tooltip = "Select the Qi zone you want to train in.", Callback = function(nk)
  if nj[nk] then
    al = nj[nk]
  end
end})
local nm = false
gz:AddToggle("AutoCultivateToggle", {Text = "Enable Auto Cultivate", Tooltip = "Teleports to the selected Qi Zone and automatically cultivates with tap boosts.", Callback = function(nn)
  nm = nn
  if nm then
    task.spawn(function()
      ay(l.Character)
      task.wait(0.2)
      local no = {buffer.fromstring("\6")}
      k.ByteNetReliable:FireServer(unpack(no))
      while nm do
        local np = l.Character
        if np then
          local nq = np:FindFirstChild("Humanoid")
          if (nq and (nq.Health > 0)) then
            local nr = (((nq.Health / nq.MaxHealth)) * 100)
            if (nr < 12) then
              nq.Health = 0
              l.CharacterAdded:Wait()
              task.wait(1)
              ay(l.Character)
              task.wait(0.2)
              local ns = {buffer.fromstring("\6")}
              k.ByteNetReliable:FireServer(unpack(ns))
            else
              local nt = {buffer.fromstring("\n\v\0Cultivating")}
              k.ByteNetReliable:FireServer(unpack(nt))
              task.wait(0.5)
            end
          else
            task.wait(0.5)
          end
        else
          task.wait(0.5)
        end
      end
    end)
  else
    local nu = {buffer.fromstring("\6")}
    k.ByteNetReliable:FireServer(unpack(nu))
  end
end})
local nv = 100
gz:AddSlider("BreakthroughChanceSlider", {Text = "Breakthrough Chance %", Min = 1, Max = 100, Default = 70, Rounding = 0, Tooltip = "Set the minimum success rate you want before attempting to breakthrough.", Callback = function(nw)
  nv = nw
end})
local nx = false
gz:AddToggle("AutoBreakthroughToggle", {Text = "Enable Auto Breakthrough", Tooltip = "Triggers Qi and Body breakthroughs based on your selected chance.", Callback = function(ny)
  nx = ny
  task.spawn(function()
    while nx do
      local nz = df()
      if nz then
        if (nz.Qi and nz.Realm) then
          local oa = ej(nz.Realm)
          local ob = math.floor((oa * ((nv / 100))))
          if (nz.Qi >= ob) then
            local oc = {buffer.fromstring("\4\0")}
            k.ByteNetReliable:FireServer(unpack(oc))
            task.wait(2.5)
          end
        end
        local od = (nz.Blood or nz.Health)
        if (od and nz.Body) then
          local oe = eo(nz.Body)
          local of = math.floor((oe * ((nv / 100))))
          if (od >= of) then
            local og = {buffer.fromstring("\14\0")}
            k.ByteNetReliable:FireServer(unpack(og))
            task.wait(2.5)
          end
        end
      end
      task.wait(1)
    end
  end)
end})
local oh = {60, 300, 600, 900, 1200, 1800, 3600, 5400, 7200}
for oi, oj in ipairs(oh) do
  hd:AddDropdown(("PlaytimeTier_" .. oj), {Text = (oj .. " Seconds"), Values = g.playtimeRewards[oj], Default = g.selectedRewards[oj], Multi = false, Callback = function(ok)
    g.selectedRewards[oj] = ok
  end})
end
local ol = false
hd:AddToggle("AutoPlaytimeRewardsToggle", {Text = "Auto Claim Rewards", Tooltip = "Automatically claims your selected playtime rewards.", Default = false, Callback = function(om)
  ol = om
  if ol then
    h:Notify("🎁 Auto Claim Playtime Rewards Enabled!")
    task.spawn(function()
      while ol do
        for on, oo in ipairs(oh) do
          if not ol then
            break
          end
          local op = g.selectedRewards[oo]
          local oq = fc(oo, op)
          pcall(function()
            k.ByteNetReliable:FireServer(unpack(oq))
          end)
          task.wait(0.5)
        end
        task.wait(60)
      end
    end)
  else
    h:Notify("🛑 Auto Claim Playtime Rewards Disabled!")
  end
end})
local os = false
hd:AddToggle("AutoEventPlaytimeRewardsToggle", {Text = "Auto Claim Event Playtime", Tooltip = "Automatically claims the special Event playtime rewards.", Default = false, Callback = function(ot)
  os = ot
  if os then
    h:Notify("🎆 Auto Claim Event Playtime Enabled!")
    task.spawn(function()
      while os do
        for ou, ov in ipairs(fh) do
          if not os then
            break
          end
          local ow = fi(ov)
          pcall(function()
            k.ByteNetReliable:FireServer(unpack(ow))
          end)
          task.wait(0.5)
        end
        task.wait(60)
      end
    end)
  else
    h:Notify("🛑 Auto Claim Event Playtime Disabled!")
  end
end})
hc:AddButton({Text = "Copy Current Location", DoubleClick = false, Tooltip = "Copies your exact character CFrame to your clipboard.", Func = function()
  local ox = l.Character
  local oy = (ox and ox:FindFirstChild("HumanoidRootPart"))
  if oy then
    local oz = string.format("CFrame.new(%f, %f, %f, %f, %f, %f, %f, %f, %f, %f, %f, %f)", oy.CFrame:GetComponents())
    if setclipboard then
      setclipboard(oz)
      h:Notify("📋 Location copied to clipboard!")
    else
      print(("Your location format for script: " .. oz))
      h:Notify("⚠️ Executor doesn't support clipboard. printed to F9 log.")
    end
  else
    h:Notify("❌ HumanoidRootPart not found!")
  end
end})
hc:AddButton({Text = "Redeem All Codes", DoubleClick = true, Tooltip = "Automatically sends packets for every known code to claim rewards.", Func = function()
  task.spawn(function()
    h:Notify("🎁 Redeeming codes... Check top left!")
    for pa, pb in ipairs(g.gameCodes) do
      local pc = ez(pb)
      pcall(function()
        k.ByteNetReliable:FireServer(unpack(pc))
      end)
      task.wait(2)
    end
    h:Notify("✅ Finished redeeming all codes!")
  end)
end})
j:SetLibrary(h)
i:SetLibrary(h)
i:SetIgnoreIndexes({"MenuKeybind"})
j:SetFolder("YHub")
i:SetFolder("YHub/CultivationDaoOnlineV2")
j:ApplyToTab(gw)
i:BuildConfigSection(gw)
i:LoadAutoloadConfig()
